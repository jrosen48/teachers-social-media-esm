---
title: "Output"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_float: true
params:
   overall_time_point_df: ""
   purposes: ""
   purposes_m: ""
   stress_m: ""
   stress_data: ""
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, cache = FALSE)

response_df <- params$overall_time_point_df
purposes <- params$purposes

purposes_m <- params$purposes_m

stress_data <- params$stress_data
stress_m <- params$stress_m
```

# Descriptives

## Response Rate

```{r}
response_df %>% 
  mutate(prop = n/14) %>% 
  mutate_if(is.double, round, 3) %>% 
  select(everything(), n, prop) %>% 
  knitr::kable()
```

# RQ 1

## Descriptives

### Purposes

```{r}
purposes %>% 
  mutate_if(is.double, round, 3) %>% 
  knitr::kable()
```

### Purposes figures

```{r}
knitr::include_graphics(here("img", "all-teachers-how.png"))
```

# RQ 2

## Purposes Models

```{r}
purposes_m %>% 
  mutate_if(is.double, round, 3) %>% 
  knitr::kable()
```

# RQ 3

```{r}
stress_data %>% 
  set_names(c("recipient_email", "time_point", 
              "parenting", "family", "friendships", "mental health", "physical health", "covid - self", "covid - family", "covid - community",
              "videoconference - work", "videoconference - children", "hardware", "lms", "email", "social media",
              "survey_period")) %>% 
  filter(!is.na(recipient_email)) %>% 
  mutate_at(3:16, my_func) %>% 
  mutate_at(3:16, replace_na, 0) %>% 
  skimr::skim()
```

## Stress Models

```{r}
stress_m %>% 
  mutate_if(is.double, round, 3) %>% 
  knitr::kable()
```

# Appendix

## Stress descriptives (not run)

```{r, eval - FALSE}
stress_data %>% 
  set_names(c("recipient_email", "time_point", 
              "parenting", "family", "friendships", "mental health", "physical health", "covid - self", "covid - family", "covid - community",
              "videoconference - work", "videoconference - children", "hardware", "lms", "email", "social media",
              "survey_period")) %>% 
  knitr::kable()
```